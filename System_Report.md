# التقرير الفني الشامل: المنصة الوطنية للرقابة والتفتيش الصحي - نقابة التمريض العراقية

## 1. أهداف المشروع ورؤيته المستقبلية

**الهدف الرئيسي:** 
بناء منصة رقمية مركزية متكاملة (ERP & GIS System) لتنظيم، تتبع، وتوثيق كافة أعمال لجان الكشف الميداني التابعة لنقابة التمريض العراقية في كافة المحافظات. تهدف المنصة إلى القضاء على التعاملات الورقية، حماية المهنة من الدخلاء، إحكام الرقابة على المؤسسات الصحية (الحكومية والأهلية)، وضبط الإيرادات والمخالفات بشفافية تامة.

**الرؤية المستقبلية:**
- **الدفع الإلكتروني (FinTech):** تحويل جباية الغرامات ورسوم التجديد إلى نظام رقمي متصل ببوابات الدفع الوطنية (مثل الدفع عبر بطاقات الماستر كارد والمحافظ الإلكترونية).

---

## 2. البنية التحتية والتقنيات المستخدمة (Tech Stack)

تم بناء المشروع وفق أحدث معايير تطوير واجهات الويب التقدمية (SPA / PWA)، مما يضمن سرعة الاستجابة وعمل النظام كبرنامج أصلي على الحواسيب والهواتف.

- **بيئة العمل الأساسية:** `React.js` مع `TypeScript` (لضمان قوة الكود ومنع الأخطاء).
- **أداة البناء:** `Vite` (لسرعة البناء والتطوير).
- **التصميم والواجهات (UI/UX):** `Tailwind CSS` لتصميم متجاوب وحديث، مع تخصيص نظام Theme مرن يتيح التبديل بين الألوان الفاتحة والداكنة، والأنماط الزجاجية (Neumorphism / Glassmorphism) كما في صفحة الدخول.
- **الأيقونات:** `Lucide React` و `FontAwesome`.
- **نظام الخرائط (GIS):** مكتبة `Leaflet.js` مع `React-Leaflet` لعرض خرائط تفاعلية وتوزيع جغرافي للمؤسسات والزونات.
- **التواصل (Backend):** 
  - محاكاة لخادم محلي (Node.js/Express) بملف `server.ts` للتعامل مع طلبات API.
  - إعداد مسبق لقواعد بيانات `Firebase` (Firestore / Auth) للتشغيل السحابي.

---

## 3. الشرح التفصيلي للتبويبات (Modules)

يتميز النظام بهيكلية تبويبات مرنة، تعتمد على صلاحيات المستخدم (Role-Based Access Control).

### 3.1. لوحة القيادة (Dashboard - `dashboard`)
- **الوظيفة:** مركز القيادة لمتخذي القرار.
- **المحتوى:** إحصائيات حية (عدد الكشوفات، إجمالي الغرامات المحصلة، المؤسسات غير المرخصة، عدد الكوادر المقاطعة). رسوم بيانية توضح نشاط لجان التفتيش وتوزيعها على المحافظات.

### 3.2. الخريطة التفاعلية (GIS Map - `gis_map`)
- **الوظيفة:** نظام تتبع مكاني متقدم.
- **المحتوى:** يعرض مواقع المؤسسات الصحية كنقاط على خريطة العراق، وتلوين الزونات الجغرافية بناءً على نسبة الإنجاز التفتيشي. يتيح النقر على أي مؤسسة لإصدار أمر تفتيش مباشر لها.

### 3.3. إدارة الزونات والمقاطعات (`zone_manager`)
- **الوظيفة:** تقسيم المحافظات إدارياً.
- **المحتوى:** رسم أو تحديد مناطق جغرافية معينة وربطها بلجان تفتيش مخصصة، مما يمنع تداخل الصلاحيات بين اللجان (مثال: زون الكرخ، زون الرصافة).

### 3.4. المستخدمون ولجان التفتيش (`users_management`)
- **الوظيفة:** إدارة الموارد البشرية وتشكيل اللجان.
- **التبويبات الفرعية:**
  1. **إدارة الحسابات:** إضافة مفتشين ومدراء، تحديد صلاحياتهم، وربطهم بأرقام هواتفهم لاستعادة المرور (عبر واتساب).
  2. **تشكيل اللجان:** تجميع المفتشين في "لجنة كشف" برئاسة مفتش أقدم، وتخصيص باجات وأرقام تعريفية للجنة.

### 3.5. المؤسسات الصحية (`facilities`)
- **الوظيفة:** قاعدة بيانات المراكز والمستشفيات.
- **المحتوى:** سجل شامل لكل مستشفى/عيادة/مذخر، يحتوي على حالة الترخيص، اسم المالك، الموقع، وسجل الزيارات السابقة التي قامت بها النقابة.

### 3.6. المهام والتكاليف (`assignments`)
- **الوظيفة:** نظام إدارة سير العمل (Workflow & Dispatching).
- **المحتوى:** إنشاء أوامر تكليف للجان للنزول الميداني. تحويل حالة التكليف (قيد الانتظار -> جاري التنفيذ -> مكتمل).

### 3.7. سجل الكوادر التمريضية (`nurses`)
- **الوظيفة:** مقاطعة بيانات العاملين (Database Cross-matching).
- **المحتوى:** قاعدة بيانات تمريضية. يتم التحقق من هويات النقابة أثناء الكشف لضبط الدخلاء والكوادر الوهمية أو تاركي العمل.

### 3.8. المخالفات والإغلاقات (`violations`)
- **الوظيفة:** نظام إنفاذ القانون.
- **المحتوى:** أرشفة كافة الغرامات، الإنذارات، وقرارات الإغلاق (بالشمع الأحمر) الموجهة للمؤسسات المخالفة لتعليمات نقابة التمريض.

### 3.9. الإدارة المالية والجباية (`finance`)
- **الوظيفة:** محاسبة وشفافية مالية.
- **المحتوى:** إصدار وصولات القبض الإلكترونية للغرامات، اشتراكات النقابة، رسوم تجديد الإجازات، ومتابعة ميزانية الفروع بالمحافظات.

### 3.10. غرفة العمليات المركزية (`operations_chat`)
- **الوظيفة:** تواصل مشفر وآمن.
- **المحتوى:** شات داخلي بين اللجان الميدانية وغرفة العمليات في المقر العام. يمكن للجان طلب الإسناد الأمني أو القانوني فوراً أثناء تواجدهم في الميدان.

---

## 4. تطبيق المفتش الميداني (Field Inspector Mobile - `field_inspector`)

تم تصميم هذا الجزء ليعمل كتطبيق هاتف (PWA) في أيدي المفتشين أثناء تجوالهم.
- **الواجهة الأمامية:** شاشة هاتف محاكية (Mobile Frame) بتصميم مريح يسهل استخدامه بيد واحدة.
- **الآليات التقنية:**
  1. **الكشف الرقمي (Digital Checklist):** يعبئ المفتش استمارة كشف مقسمة (البنى التحتية، الأجهزة، الكوادر، النظافة).
  2. **مقاطعة الهويات (Identity Check):** ماسح/مدخل لأرقام هويات النقابة، يخبر المفتش فوراً إذا كان الكادر الذي أمامه وهمياً أو غير مسجل.
  3. **إرفاق الأدلة (Media Upload):** التقاط صور للمخالفات عبر الكاميرا ورفعها مباشرة للتقرير.
  4. **GPS Tracking:** تسجيل الموقع الجغرافي (Lat/Lng) لحظة إرسال التقرير لضمان تواجد المفتش فعلياً في موقع المؤسسة.
  5. **التوقيع الرقمي:** إغلاق الكشف ببصمة رقمية للمفتش، وإرسال البيانات للـ Dashboard.

---

## 5. الآليات المستخدمة في البرمجة (Programming Mechanisms)

إذا أردت تمرير المشروع لنموذج ذكاء اصطناعي آخر، يجب أن يراعي الآليات التالية في البناء:

1. **إدارة الحالة (State Management):** 
   الاعتماد الكثيف على `React Context` أو تمرير الخواص عبر مستوى عالٍ (Lifting State Up في `App.tsx`). البيانات تحفظ في الـ `localStorage` لضمان عدم ضياعها عند تحديث الصفحة (Persisted State).
2. **محاكاة الواجهة الخلفية (Mock API Integration):**
   استخدام واجهة `fetch` للاتصال بـ `server.ts` عبر روابط مسارات `/api/...`. جميع مسارات الـ API تعتمد مسار الإرجاع بنسق JSON.
3. **نظام الصلاحيات (RBAC - Role Based Access Control):**
   تخصيص الواجهات برمجياً عبر فحص قيمة `userRole` في `localStorage`. (مثال: إذا كان `ADMIN` تظهر له الإدارة المالية، وإذا كان `FIELD_INSPECTOR` يُوجه مباشرة لتطبيق الهاتف).
4. **تعدد الثيمات (Theming & Styling):**
   استخدام `Tailwind CSS` مع سياق مخصص (`LanguageThemeContext.tsx`) لتغيير متغيرات CSS، مما يسمح بتغيير لون الشريط الجانبي (Sidebar) وتطبيق التصميم الزجاجي (Neumorphism) كما طُبق في صفحة الدخول `login.html`.
5. **التعامل مع الاستمارات (Form Handling):**
   التحقق من البيانات (Validation) قبل الإرسال، كإجبار إدخال رقم الهاتف في شاشة إدارة المستخدمين، وعمل رسوم متحركة (Animations) عند وجود أخطاء.

---

**ختاماً:** هذا المشروع يمثل نظام تخطيط موارد مؤسسات (ERP) متقدم للغاية ومصمم خصيصاً ليناسب الهيكل التنظيمي لنقابة التمريض العراقية. التصميم المنفصل للواجهات (Modular Design) يسمح بتوسيع النظام مستقبلاً ليشمل خدمات المواطنين، وبوابات الشكاوى، وربط البنوك ببساطة تامة.